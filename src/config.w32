// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("haojing", "for haojing support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("haojing", "enable haojing support", "no");

if (PHP_HAOJING != "no") {
	EXTENSION("haojing", "haojing.c");
}

